GEMINY_API_KEY=
# Backend server URL (override if running server on a different port)
#VITE_SILVIE_SERVER_URL=http://localhost:8080
# Google OAuth2 credentials (Desktop app type)
# Create at: https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
