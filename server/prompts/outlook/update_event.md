Update an existing Outlook calendar event. Requires the event ID (obtained
via `get_outlook_calendar_events`). Any combination of subject, start/end
times, location, body, and attendees can be updated.

Always summarize the changes you are about to make and wait for explicit user
confirmation before calling this tool.
