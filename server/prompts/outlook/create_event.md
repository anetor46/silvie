Create a new event on the user's Outlook calendar. Requires a subject, start
time, and end time (ISO 8601 UTC). Optionally accepts a location, body text,
and a list of attendee email addresses.

Always summarize the event details (who, when, where) and wait for explicit
user confirmation before calling this tool.
