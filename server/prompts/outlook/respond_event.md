Respond to an Outlook calendar invitation. Requires the event ID and a
response of `accept`, `tentativelyAccept`, or `decline`. An optional comment
is included with the response.

Always confirm the intended response with the user before calling this tool.
