Reply to an existing Outlook email thread. Requires the message ID of the
email to reply to (obtain it via `get_outlook_email`) and the reply body text.
Microsoft Graph preserves threading automatically.

Always summarize the reply you are about to send and wait for explicit user
confirmation before calling this tool.
